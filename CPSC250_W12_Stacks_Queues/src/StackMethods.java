/**
 * Methods that act on stacks
 * 
 * @author <student>
 * @version 0
 * 
 *
 */

public class StackMethods {

    /**
     * Creates a separate copy of the stack (same references to elements, so not
     * deep)
     * 
     * Make sure the original stack is not changed!
     * 
     * @param <T>
     *            type for stack
     * @param stack
     *            to copy
     * @return stack containing same references as original stack
     */
    public static <T> SimpleStack<T> copyStack(SimpleStack<T> stack) {

        // @todo - fix this

        return null;

    }

    /**
     * Return true if stack contains an element (equals) false otherwise.
     * 
     * The original stack should not be modified
     * 
     * @param <T>
     *            type for stack
     * @param stack
     *            to check
     * @param value
     *            to check
     * @return true if value is contained in stack; false otherwise
     */
    public static <T> boolean contains(SimpleStack<T> stack, T value) {

        // @todo - fix this

        return false;

    }

}
